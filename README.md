


```
to install:
 - npm ci

to run:
 - npm run dev
 - http://localhost:3000
 

vercel deployment:
 - make sure to add env variable: NEXT_PUBLIC_API_URL (should point to url of the fastapi app)


```